def score_grade(score):
    if score <= 10:
        return "A"

# def exam_scoe(sel_sub):
#     subject = Mark.objects.filter(subject = sel_sub)
#     return int(subject.values('exam_score')[0]['exam_score'][:-1])
